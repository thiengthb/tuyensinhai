"use client";
import React from "react";

export default function SideAdvertises({ width, height }) {
  return (
    <div style={{ width: `${width}px`, height: `${height}px` }} className="bg-[#D9D9D9]">
    </div>
  );
}
